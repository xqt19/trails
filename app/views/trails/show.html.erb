<h1><%= @trail.name %></h1>
<%= link_to "edit", edit_trail_path(@trail) %>
<%= link_to "delete", trail_path(@trail), method: :delete, data: { confirm: "Are you sure?" } %>
<p><%= @trail.start_date %> to <%= @trail.end_date %></p>
<p>Countdown: <%= (@trail.start_date - Date.today).to_i %> more days</p>

<h2>Trail group (<%= @trail.users.count %>)</h2>

<% @trail.users.each do |collab_user| %>
  <p><%= collab_user.name %></p>
<% end %>

<h2>Planning List</h2>

<!-- add template codes here -->

<h2><%= @trail.start_date %> to <%= @trail.end_date %></h2>

<!-- add day cards here -->

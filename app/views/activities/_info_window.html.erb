<div class="animated zoomIn">
	<div class="">
		<h2><%= activity.name %></h2>
		<p class="py-2 mb-1"><%= activity.location.upcase %></p>
	</div>
	<div class="d-flex justify-content-between align-items-center mb-0">
		<p class="mb-0"><%= activity.start_time.to_datetime.strftime("%l:%M:%p") %></p>
    <p class="mb-0"><%= activity.date.strftime("%d.%m.%y") %></p>
		<p class="mb-0"><%= activity.end_time.to_datetime.strftime("%l:%M:%p") %></p>
	</div>
</div>
<style>

.mapboxgl-popup-content {
    position: relative;
    background: #fff;
    border-radius: 8px;
    box-shadow: 0 1px 5px rgba(0, 0, 0, 0.2);
    padding: 10px 10px 10px;
    pointer-events: auto;
}

</style>
